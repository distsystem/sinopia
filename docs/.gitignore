# Quarto build + environment artifacts
/_site/
/.quarto/
/_freeze/
_extensions/
pixi.lock

# Disposable discussion drafts: rendered locally with the site theme,
# never committed. The _template.qmd starter stays tracked; real <slug>.qmd
# drafts are ignored.
/drafts/*
!/drafts/_template.qmd

**/*.quarto_ipynb
